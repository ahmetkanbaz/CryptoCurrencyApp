import styled from "styled-components";

export const AccordionWrapper = styled.div`
  .accordion-button {
    background-color: #f2f8fe;
    &:not(.collapsed) {
      color: #FFFFFF;
      background: #92AEC9;
      box-shadow: none;
    }
  }
`;
